<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.uqac.lecitoyen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:noHistory="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BASE ACTIVITY -->
        <activity android:name=".BaseActivity" />

        <!-- LOGIN ACTIVITY -->


        <!-- USER ACTIVITY -->
        <activity
            android:name=".userUI.UserMainActivity"
            android:label="@string/title_activity_user" />
        <activity android:name=".userUI.newsfeed.PostActivity"
            android:windowSoftInputMode="adjustResize"/>

        <!-- USER SETTING ACTIVITY -->
        <activity
            android:name=".userUI.settings.UserSettingsActivity"
            android:launchMode="standard"
            android:parentActivityName=".userUI.UserMainActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />

        <!-- FACEBOOK LOGIN ACTIVITY -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".userUI.newsfeed.EditPostActivity" />
        <activity android:name=".userUI.profile.EditProfileActivity"></activity>
    </application>

</manifest>