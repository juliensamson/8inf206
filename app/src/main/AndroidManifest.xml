<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.uqac.lecitoyen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:noHistory="false">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BASE ACTIVITY -->
        <activity android:name=".BaseActivity" />

        <!-- LOGIN ACTIVITY -->
        <activity
            android:name=".Auth.EmailPasswordActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Auth.ResetPasswordActivity"
            android:parentActivityName=".MainActivity" />

        <!-- USER ACTIVITY -->
        <activity
            android:name=".User.UserActivity"
            android:label="@string/title_activity_user" />

        <!-- USER SETTING ACTIVITY -->
        <activity
            android:name=".User.UserSettings.UserSettingsActivity"
            android:parentActivityName=".User.UserActivity"
            android:launchMode="standard" />
        <activity
            android:name=".User.UserSettings.ChangePasswordActivity" />
        <activity
            android:name=".User.UserSettings.ChangeEmailActivity" />
        <activity
            android:name=".User.UserSettings.DeleteAccountActivity" />

        <!-- FACEBOOK LOGIN ACTIVITY -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>

</manifest>